# كتالوج ألوان — مصنع أخشاب الغابة

مستودع **مستقلّ** لكتالوج ألوان الأبواب والتشطيبات الخشبيّة (Color Collection 2026).

> **تنبيه هويّة:** هذا المستودع يخصّ **مصنع أخشاب الغابة** فقط.
> إذا كان الطلب عن **إيناء / EEINA** (منصّة التغذية) أو أيّ مشروع آخر، فهذا هو المستودع الخطأ —
> اطلب المستودع الصحيح عبر `add_repo` بدل العمل هنا أو الإبلاغ بعدم وجود معلومات.

## المحتوى

- `index.html` — الكتالوج كاملاً في ملفّ واحد (~5.8 ميجابايت): 24 صفحة بمقاس A4، و76 لوناً في قسمين («الوان مساكن» و«الوان مصنع الرياض»).
- `logo.jpg` — الشعار الأصليّ بدقّة عالية (5278×4549)، غير مستخدَم في الكود حالياً.

لا توجد أدوات بناء ولا تبعيّات — يُفتح `index.html` في المتصفّح مباشرة.

## بنية `index.html`

| النطاق | المحتوى |
|---|---|
| 9–246 | CSS كاملاً (متغيّرات الهويّة في `:root`) |
| 253–331 | الغلاف + غلاف القسم الأوّل |
| 333–1713 | صفحات الألوان — شبكة 2×2، أربعة ألوان لكلّ صفحة |
| 1715–1793 | صفحة الختام + زرّ واتساب + العرض المكبّر |
| 1796 | مصفوفة `colors` — سطر واحد بحجم 2.7 ميجابايت |

## عيوب معروفة (مُقاسة 2026-09-14، غير مُصلحة)

1. **كلّ صورة مخزّنة نسختين** — في بطاقة الصفحة وفي مصفوفة `colors`. 177 صورة base64، الفريد منها 77 فقط ⇒ 2.84 ميجابايت مكرّرة.
2. **فهارس ثابتة** — `openLightbox(0)` حتّى `(75)` مربوطة بترتيب المصفوفة؛ إضافة لون في الوسط تكسر ما بعده.
3. **الشعار مكرّر 25 مرّة**، ورقم الجوّال في 3 مواضع، و21 رقم صفحة مكتوباً يدويّاً.
4. **18 صورة بالاتّجاه الأفقيّ** (700×315) بين 58 عموديّة (334×700) ⇒ تُقصّ بشدّة تحت `object-fit:cover`. كلّها في قسم «الوان مصنع الرياض».
5. **لا وسوم Open Graph** ⇒ معاينة الرابط عند المشاركة تظهر فارغة.

## عند التعديل

إضافة لون تتطلّب اليوم: تحويل الصورة إلى base64، ولصقها في موضعين، وإعادة ترقيم الفهارس والصفحات.
قبل أيّ تعديل موسّع، الأفضل فصل الصور في مجلّد ونقل البيانات إلى ملفّ JSON واحد.
